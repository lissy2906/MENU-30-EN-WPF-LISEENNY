<UserControl x:Class="EjerciciosWPFLissy.Ejercicio10"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="200" Width="500">
    <StackPanel Margin="20">
        <TextBlock Text="Ejercicio 10: Mayor de edad" FontWeight="Bold" FontSize="16"/>

        <TextBox x:Name="txtEdad" Margin="0,10"/>
        <Button Content="Verificar" Width="100" Click="BtnVerificar_Click"/>
        <TextBlock x:Name="txtResultado" Margin="0,15,0,0" FontWeight="SemiBold"/>
    </StackPanel>
</UserControl>

